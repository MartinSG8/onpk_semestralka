apiVersion: v1
kind: Service
metadata:
  name: {{ include "onpk-backend.fullname" . }}
  labels:
    {{- include "onpk-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "onpk-backend.selectorLabels" . | nindent 4 }}
