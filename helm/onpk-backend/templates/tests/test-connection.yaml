apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onpk-backend.fullname" . }}-test-connection"
  labels:
    {{- include "onpk-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "onpk-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
