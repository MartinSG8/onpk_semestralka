apiVersion: v1
kind: Service
metadata:
  name: {{ include "onpk-frontend.fullname" . }}
  labels:
    {{- include "onpk-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "onpk-frontend.selectorLabels" . | nindent 4 }}
