# terraform
```
terraform init
terraform plan
terraform apply
terraform destroy
```
# docker
```
docker ps
docker rmi
docker run
docker build
```
# helm
```
k apply -f <file-name>
k delete -f <file-name>

helm install
helm update

```
# minikube
```
m addons list
minikube addons enable ingress

```
# kubectl
```
k apply -f <file-name>
k delete -f <file-name>
k get pods
```
